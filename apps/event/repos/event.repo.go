/*
generated by comer,https://github.com/imoowi/comer
Copyright © 2023 jun<simpleyuan@gmail.com>
*/
package repos

import (
	"errors"

	"github.com/gin-gonic/gin"
	"github.com/imoowi/live-stream-server/apps/event/models"
	"github.com/imoowi/comer/components"
	"github.com/imoowi/live-stream-server/global"
	"github.com/imoowi/comer/utils/response"
	"gorm.io/gorm"
)

type EventRepo struct {
	Db *components.MysqlODM
}

func newEventRepo() *EventRepo {
	return &EventRepo{
		Db: global.MysqlDb,
	}
}
func init() {
	global.DigContainer.Provide(newEventRepo)
}


func (r *EventRepo) All(c *gin.Context, query *models.EventQuery) (res []*models.Event, err error) {
	db := r.Db.Client
	if query.Key != `` {
		db = db.Where("`key`=?", query.Key)
	}

	err = db.Find(&res).Error
	if errors.Is(err, gorm.ErrRecordNotFound) {
		err = nil
	}
	return
}

func (r *EventRepo) PageList(c *gin.Context, query *models.EventQuery) (res *response.PageList, err error) {
	db := r.Db.Client
	var users []*models.Event

	if query.SearchKey != `` {
		db = db.Where(`name LIKE ?`, `%`+query.SearchKey+`%`)
	}
	offset := (query.Page - 1) * query.PageSize
	db = db.Offset(int(offset)).Limit(int(query.PageSize))
	// db=db.Order(`name desc`)
	err = db.Find(&users).Error

	var count int64
	db.Offset(-1).Limit(-1).Count(&count)

	res = &response.PageList{
		List:  users,
		Pages: response.MakePages(count, query.Page, query.PageSize),
	}
	return
}

func (r *EventRepo) One(c *gin.Context, id uint) (user *models.Event, err error) {
	db := r.Db.Client
	err = db.Where(`id=?`, id).First(&user).Error
	return
}

func (r *EventRepo) OneByName(c *gin.Context, name string) (user *models.Event, err error) {
	db := r.Db.Client
	err = db.Where(`name=?`, name).First(&user).Error
	return
}
func (r *EventRepo) Add(c *gin.Context, model *models.Event) (newId uint, err error) {
	db := r.Db.Client
	db = db.Create(&model)
	err = db.Error

	newId =  model.ID
	return
}

func (r *EventRepo) Update(c *gin.Context, model *models.Event, id uint) (updated bool, err error) {
	if id == 0 {
		updated = false
		err = errors.New(`pls input id`)
		return
	}
	model.ID = uint(id)
	db := r.Db.Client
	err = db.Omit(`created_at`).Save(&model).Error
	if err == nil {
		updated = true
	}
	return
}

func (r *EventRepo) Delete(c *gin.Context, id uint) (deleted bool, err error) {
	if id == 0 {
		deleted = false
		err = errors.New(`pls input id`)
		return
	}
	db := r.Db.Client
	model, err := r.One(c, id)
	if err != nil {
		return
	}
	if model.ID == 0 {
		err = errors.New(`obj not exist`)
		return
	}
	err = db.Delete(&model).Error
	if err == nil {
		deleted = true
	}
	return
}
